"""
Stage 1: extraction (PDF -> structured JSON -> enriched).

Minimal __init__ to avoid pulling in heavyweight client deps.
Use explicit imports:
    from pipeline.stage1_extraction.orchestrator import DocumentProcessor
"""
